remote_file:
  host: www.tuoshecx.com
  port: 80
  file: sw-port.txt
  ssl: false
shadowsocks:
# os is immortalwrt、macos、windows、Linux、ubuntu
  os: immortalwrt
  config: /etc/config/passwall
  command: /etc/init.d/passwall restart